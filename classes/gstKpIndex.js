class GSTKpIndex {
    constructor(
        observedTime,
        kpIndex,
        source
        ) {
            this.observedTime = observedTime;
            this.kpIndex = kpIndex;
            this.source = source;
        }
}

module.exports.GSTKpIndex = GSTKpIndex;